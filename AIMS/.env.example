PORT=5000

# Shared dev database is a managed MySQL instance on Aiven (cloud), not a
# local MySQL install - ask the Database Engineer for these values via a
# secure channel (not chat/email). Also requires the SSL CA certificate at
# database/config/ca.pem to connect (see database/config/config.js for how
# it's loaded).
DB_HOST=
DB_PORT=
DB_NAME=aims_db
DB_USER=
DB_PASSWORD=
DB_SSL=true

JWT_SECRET=

AI_SERVICE_URL=http://localhost:8000